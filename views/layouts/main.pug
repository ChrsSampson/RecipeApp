// main layout

doctype html
html

head
    title=title
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/stylesheets/index.css')
    block head 

body
    if(flash)
        div(class='flash-container')
            div(class=flash.type || 'info')
            h5=flash.message

    block content
    footer
        block foot